# Copy to .env and fill in real values. NEVER commit the real .env.
RDP_HOST=host.example.com
RDP_USER=your-username
RDP_PASSWORD=your-password
RDP_DOMAIN=YOUR_DOMAIN
# optional tuning:
# RENDER_WAIT=8
# HOLD_AFTER=20
